apiVersion: v1
kind: Service
metadata:
  name: flask-hello-world
spec:
  selector:
    app: flask-hello-world
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30001
  type: NodePort